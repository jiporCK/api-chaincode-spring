spring:
  application:
    name: testing-api
  datasource:
    url: jdbc:postgresql://localhost:5432/offchainstorage
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9090
